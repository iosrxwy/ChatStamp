---
name: tc
description: 只改当前这条对话的标题。日期用创建时间（哪天开的这条用哪天）。
---

# /tc

只改**当前这一条**。`MMDD` 用这条对话的**创建时间**。

1. 现成标题已经能看懂主题：只套成 `MMDD｜类型｜主题`，然后 `rename_chat`。
2. 标题看不懂或语言不符：看我发的消息和你的回复总结，去掉代码。
3. 还是看不懂：保留原名。

类型：功能、设计、修复、优化、发布、探索、文档、研究。中文用户写中文主题，不要中英混排。只改标题。
